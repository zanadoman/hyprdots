#!/bin/bash

qemu-system-x86_64 \
    -accel kvm \
    -hda "$HOME/Qemu/tiny10_personal.img" \
    -cpu host \
    -smp $(nproc) \
    -display sdl,gl=on \
    -device virtio-gpu-gl-pci \
    -m $(expr $(free -m | grep -Po 'Mem:\s+\K\d+') / 5)M \
    -audiodev pipewire,id=snd0 \
    -device intel-hda \
    -device hda-duplex,audiodev=snd0 \
    -nic user,hostfwd=tcp::2222-:22
